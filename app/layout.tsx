import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Road Asset Tracker",
    template: "%s · Road Asset Tracker",
  },
  description:
    "Inventory and inspection system for road assets — signboards, traffic lights, stop lights, road lamps and more.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
    >
      <body className="min-h-dvh bg-zinc-100 font-sans text-zinc-900 antialiased">
        {children}
      </body>
    </html>
  );
}
